<!-- This is my first demo

>> Project title: First demo
    It is my 1st demo project which include a file and uploaded in Github. 
    In this file, it displayed a message of 'Hello World!'.

>> How to run this file:

>>Download this zip file and unzip it.
>>Select the folder and open cmd.
    >>In cmd add commands to execute the file:
        git config --global user.nam "vaishnavisaldur"
        git config --global user.email "vaishnavisaldur@gmail.com"
        git init (it displayed .git file in folder)
        git remote add origin https://github.com/vaishnavisaldur/Demo1.git
        
>>Open new terminal in vs code in project:
        git pull origin main
        git status 
        git add . (or) git add index.html
        git status
        git commit -m
        git push
        git push --set-upstreeam origin master
 -->

<!-- git add . (or)
git add index.html style.css
git commit -m "Added project files"
git push origin main
git push origin master
git status -->